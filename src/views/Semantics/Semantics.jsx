import React from 'react'
import Buttons from '@components/Semantics/Buttons/Buttons'

const Semantics = () => {
  return (
    <div style={{padding: '10px'}}>
      <Buttons/>
    </div>
  )
}

export default Semantics